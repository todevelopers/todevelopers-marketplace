{
  "mcpServers": {
    "test-mcp": {
      "command": "sh",
      "args": [
        "-c",
        "npx -y mcp-remote http://localhost:${TEST_MCP_PORT:-3001}/test/"
      ],
      "env": {
        "TEST_MCP_PORT": "3001"
      }
    }
  }
}
