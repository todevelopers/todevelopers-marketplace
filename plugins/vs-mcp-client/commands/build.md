---
name: build
description: Build the Visual Studio solution using VS MCP Server
---

Use the `ExecuteCommand` tool from the vs-mcp MCP server to build the solution.

Call: `ExecuteCommand` with command `Build.BuildSolution`

After the command completes, report whether the build succeeded or failed, and surface any errors or warnings returned by the tool.
